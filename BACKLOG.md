# Alef Bet — Backlog

> Выполненное — удалять. Новое — в нужную секцию.
> Восстановлено 07.08.2026 из внешней копии; пункты ниже перепроверены по реальному коду там, где это было возможно статически (см. пометки).

---

## 🩹 План: докрутить уровни 1–4, прежде чем начинать 5–6

> Добавлено 07.08.2026 по факту аудита `grammarLessons.js` (56 уроков, парсинг данных). Вывод: то,
> что курс числится «пройденным до уровня 4» / «58 из 108», не значит, что этот контент
> полноценен. Порядок работы — сначала долечить то, что уже стоит в графе как done/available,
> и только потом наращивать новые уровни (5–6, см. «📚 Курс» ниже).

**Этап 1 — уровень 4 (приоритет, самое запущенное):** ✅ закрыто 08.08.2026 (кроме озвучки — она отдельно, см. ниже)
- [x] Прогнать все 18 уроков (C5.1, CH3.1–3.2, G3.1–3.6, M3.1–3.3, Q1.1–1.2, SL1.1–1.4) через approve — все `approved:true`, build + смоук (200/200) зелёные
- [x] Вычитать 62 draft-примера и снять флаг — вычитано вручную (грамматика/огласовки/RTL), флаг `draft:true` снят у всех
- [x] Добить `practiceItems` с 6 до 8 на урок — все 18 уроков теперь 8/8 (у G3.5-синтеза было уже 8, не трогали)
- [x] Добить `examples` минимум до 5–6 на урок — все 18 уроков теперь 5–6 (было ровно 4)
- [x] Добавить `dialogueCards` хотя бы в часть уроков — добавлено в 4 из 18 (M3.1, Q1.1, Q1.2, C5.1, по 3 карточки), формат как в C1/C2
- [x] Озвучить примеры уровня 4 — закрыто 08.08.2026, см. «🔊 Долг по озвучке» ниже

**Этап 2 — точечный долг в уровнях 1–3:** ✅ закрыто 08.08.2026 (включая озвучку)
- [x] Аудио отсутствовало у 27 примеров в 10 уроках — закрыто 08.08.2026 прогоном `python tools/tts/generate_tts.py`
- [x] `practiceItems` меньше заявленного в `practiceCount` — закрыто 08.08.2026: C0 8→10, M1.2 8→10, M1.3 8→12 (все догнаны до заявленного числа); build + смоук (200/200) зелёные
- [x] **Бонус-фикс попутно найденного бага**: в `readingBlock` урока M1.2 слово «город» было `הֶעִיר`/«хе-ир» — противоречило и собственному правилу урока, и канонической записи в `reading.js` (`הָעִיר`/«ха-ир»). Исправлено на `הָעִיר`.
- [x] **Диалоговая практика — закрыто 08.08.2026 системно**: было 5 уроков из 34 в уровнях 1–3 (C1, G1.5, C2, M2.6, C3 — фактическая цифра, не 24/4 как было в старой записи), добавлено `dialogueCards` (по 2 карточки) в остальные 29 — теперь во всех 34 уроках уровней 1–3 есть диалог. Все карточки — только интонационные да/нет-вопросы («X? — Да/Нет, X»), без вопросительных слов מי/מה/איפה/מתי/למה/כמה — они по графу курса появляются только в Q1.1/Q1.2 (уровень 4), раньше их использовать нельзя. Лексика в каждом диалоге ограничена тем, что уже пройдено на момент урока (сверено по фактическому порядку прохождения в `curriculum.js`, не по полю `seq`, которое для части уроков — унаследованная нумерация и не совпадает с реальным порядком, напр. D1.3). Build + смоук (200/200) зелёные после каждого батча (A–H, 8 батчей).
- [x] **Решено**: D1.1 и G1.1 — тест НЕ нужен. Оба урока явно помечены в `notes` как «Урок-концепция, без теста (score=null→100)» — это осознанный дизайн (короткие уроки-мостики между темами), а не пропуск. Оставлено как есть. (G1.1 всё же получил dialogueCards наравне с остальными — диалог не тест.)

**Этап 3 — только после этого:** новые уровни 5–6 (см. «📚 Курс» ниже) и словарь (сейчас ≈600 уникальных слов при цели плана Алеф 800–1200).

---

## 🔴 Критично

- [x] **Сменить `ADMIN_SECRET` в Vercel на длинный случайный секрет** — закрыто 10.08.2026 (BUG-021, см. `BUGLOG.md`). Было 4 цифры (10 000 комбинаций, без rate-limit на `api/admin.js`); заменено владельцем на 24-байтовый случайный base64url-секрет, передеплоено. Код-часть (constant-time сравнение) была пофикшена раньше, 08.08.2026.
- [ ] **Проверить Vercel Environment Variables (прод) — `ALLOW_DEV_BYPASS` не должен быть задан.** Контекст: BUG-020 (08.08.2026, см. `BUGLOG.md`) — dev-обход подписи Telegram в `api/sync.js` работал и на боевом URL (`initData:"dev"` давал `200` в обход HMAC-проверки). Пофикшено кодом (обход теперь требует ещё и `ALLOW_DEV_BYPASS==="1"` в окружении), но у ассистента нет доступа к Vercel-дашборду без авторизации — нужно, чтобы владелец лично зашёл в Vercel → Settings → Environment Variables и убедился, что `ALLOW_DEV_BYPASS` там не выставлен (или явно = не "1") для Production. Локально для разработки — выставить `ALLOW_DEV_BYPASS=1` в `.env`.
- [x] **RLS отключён на всех 7 таблицах Supabase** — закрыто 08.08.2026 (`enable_rls_remaining_tables`). Проверено по коду: фронтенд нигде не обращается к Supabase напрямую (нет anon/publishable-ключа в `src/`), весь доступ — через `api/*.js` с `SUPABASE_SERVICE_KEY` (service role, RLS не касается) → RLS включён БЕЗ политик (deny-all для `anon`/`authenticated`) на `user_stats`, `referrals`, `events`, `daily_sessions`, `deck_words`, `user_word_progress`, `payments` — тот же паттерн, что уже был у `vocab_items`. Advisor подтвердил: ERROR-линты `rls_disabled_in_public` исчезли, остались только INFO `rls_enabled_no_policy` (ожидаемо и безопасно). Заодно поправлен WARN `function_search_path_mutable` на `update_updated_at`. Если в будущем понадобится прямой клиентский доступ (публичное чтение `deck_words`/`vocab_items` в обход Vercel-функций, realtime-лидерборд, standalone веб с Supabase Auth) — тогда добавлять точечные `CREATE POLICY` под конкретный сценарий, не раньше.
- [ ] **`api/decks.js` — верификация initData** (как в `api/sync.js`) до широкого релиза — ⚠️ подтверждено по коду 07.08.2026: в файле прямым текстом `// initData не верифицируем здесь ради краткости примера — В ПРОДЕ добавить`
- [ ] `premiumExpiresAt` — отзывать premium при истечении — ⚠️ подтверждено: `api/admin.js` умеет выставлять `premiumExpiresAt` (грант на N дней), но ни в `api/cron.js`, ни где-либо ещё нет проверки истечения — просроченный Premium никогда не гаснет сам
- [ ] `colorScheme` из `Telegram.WebApp` (Android) + флаг ручного override — частично сделано: `ThemeContext.jsx` уже читает `tg.colorScheme` и подписывается на его смену; Android-специфичного обхода и флага ручного override всё ещё нет
- [ ] PGRST125: логировать body ответа Supabase в `api/sync.js` → грепнуть Vercel Logs
- [x] «Написать в поддержку» — username бота — уточнено: кнопка ведёт на `t.me/alef_betbot` (`ProfileScreen.jsx`); тот же бот в реферальных ссылках.
- [ ] Унификация написания юзернейма бота — владелец подтвердил 10.08.2026: правильно `alef_betbot`. `ProfileScreen.jsx` и `UpdatePopup.jsx` (обновлён 10.08.2026, коммит `7f1fdd1` — кнопка «Пригласить друга») уже используют это написание; `LearnScreen.jsx:367` пока хардкодит `AlefBetBot` — фикс поставлен фоновой задачей (см. `task_6dd34590`), не завершён на момент этой записи.

## 🆕 Новое (обнаружено при сверке доков с кодом 07.08.2026)

- [ ] **AI_ENABLED=false, но UpdatePopup обещает живого ИИ-помощника** — либо включить ассистента обратно, либо убрать/переформулировать пункт «🤖 ИИ-помощник — спроси что угодно» из попапа, пока фича выключена
- [ ] Игровая клавиатура (`GameScreen.jsx`) получила 3 коммита правок подряд за один день (23.07) — стоит целевой ручной прогон на реальном устройстве, чтобы убедиться, что она стабильна, а не просто «перестала падать при последней правке»
- [ ] `ADMIN_TELEGRAM_ID` захардкожен как строка в двух местах (`api/admin.js`, `src/screens/ProfileScreen.jsx`) — вынести в одну env-переменную, чтобы не рассинхронизировалось при смене владельца
- [ ] Версионирование релизов несогласовано: коммит `247a31f` подписан `beta.V1.1.5`, хотя соседние коммиты (до и после) — `beta.V1.1.4.2`, а `package.json.version` вообще не менялся (`1.0.0`) ни разу за всю историю — определить единую схему (например, синхронизировать `package.json` с меткой в коммите) и придерживаться её
- [ ] BUG-002..006 из `BUGLOG.md` восстановлены как заглушки без дословного текста (исходные карточки существовали только вне git) — донести оригинальные формулировки и перепроверить актуальность каждого

## 🐞 Баги и вычитка контента

> Открытые баги - в `BUGLOG.md` (новое сверху). Здесь - структурная работа вокруг них.

- [x] Разобрать открытые баги BUG-001..006 (см. `BUGLOG.md`) — 07.08.2026: закрыты 001/003/004/005/006 content-фиксами в `grammarLessons.js` (build + 200/200 смоук зелёные). Открыт остался BUG-002 (UX, нискуд на телефоне) — не контентный баг, нужна проверка на устройстве.
- [ ] **Линт `grammarLessons.js`** (один скрипт): `answer ∈ options` (разово прогнан 07.08.2026 вручную — 0 нарушений на момент проверки, но скрипт не закреплён в репозитории); hebrew-строка не
  начинается с `? ! .` (частично закрыто вручную для узла C2 при фиксе BUG-004, но обёртки-проверки самой нет); повторяющееся слово в узле переведено одинаково.
  Ловит BUG-004/005 и класс BUG-001 автоматически — **превратить одноразовые проверки в постоянный скрипт**, иначе регресс не поймать.
- [ ] Те же три проверки — в смоук-сьют (регресс-защита)
- [ ] RTL-защита рендера иврита: обёртка в `dir="rtl"` / `<bdi>` (структурная защита; точечный BUG-004 в узле C2 уже пофикшен, но остальной файл не аудирован на этот класс ошибок)
- [ ] **Вычитка грамматики уровней 1-2** (считалась готовой; BUG-001/003/005/006 нашлись именно здесь — все теперь закрыты точечно, но полная построчная вычитка не проводилась)

## 📚 Курс (COURSE_MASTER_PLAN_v2)

- [ ] **Урок про местоимения перед уроком «я имею»** — Ekaterina (тг-чат «Идеи улучшений», 26.07.2026): «Хочется добавить урок про местоимения до того, как начинается урок с "я имею", так как там много примеров с разными местоимениями».
- [ ] **Уровень 5** (seq 71–86): G4 будущее время + повелительное, G5–G6 пиэль/
  хитпаэль (+метатеза), SL1.5–1.7, CH3.3, C5.2 — порции 8–10 слов
- [ ] Уровень 6 (87–108): хифиль/нифаль, SL2–SL3 (звук/буква, гзарот),
  B1 «без огласовок» / P1 «печать» / R1 «разговор» (движок готов), экзамен Алеф
- [ ] Бет (109–168): гзарот, биньяны сквозь времена, зрелый синтаксис
- [ ] Мини-узел «семьи глагола пааль» — перенесено в `BUGLOG.md` как **BUG-018** (приоритет повышен пользователем до срочности бага); полные гзарот остаются на L6/Бет
- [ ] **Вычитка draft-слов уровня 4** (огласовки/транслит) → затем TTS
- [ ] VN1.1/VN1.2 пусты; черновики r1_ch14_03, r1_m22_04, r1_ch21_04

## 🔊 Долг по озвучке (TTS)

> ✅ Полный аудит + починка — 08.08.2026 (см. `CHANGELOG.md`). Кратко: манифест дорос до 627
> записей (было 482), диалоговые карточки (83 шт. во всех 34 озвученных уроках 1–4) подключены
> к плееру через новые поля `qAudio`/`aAudio`, весь долг ниже закрыт. Гендер голоса проверен
> построчно (грамматическая сверка), ошибок не найдено. Копить новый долг сюда же, гонять
> `python tools/tts/generate_tts.py` (не забывать чистить `.tts_state.json` при правке ТЕКСТА
> записи — по гендеру идёмпотентность работает, по тексту нет, поймали 5 таких расхождений
> в этом проходе).

**Переозвучено (текст был изменён, но манифест не синхронизирован):**
- [x] `rp_11.mp3` — сверено, манифест уже содержал верный текст «אֵין בְּעַד מָה»
- [x] `ex_c0_1.mp3`/`ex_c0_2.mp3` — манифест хранил книжные אָב/אֵם, хотя урок уже показывал אַבָּא/אִמָּא — пересинхронизировано и переозвучено
- [x] `ex_c0_6.mp3`/`ex_c0_9.mp3` — сверено, манифест уже содержал верный (исправленный) текст
- [x] `r1_c2_01/02/03.mp3` — манифест хранил текст с «?» в начале строки (артефакт RTL-ввода) вместо конца — пересинхронизировано и переозвучено

**Озвучено (аудио не было):**
- [x] 99 записей уровня 4 (батчи 1–3) — сгенерировано
- [x] 5 записей, отсутствовавших в манифесте вообще: `ex_m29_6-9.mp3`, `r1_g31_06.mp3` — добавлены и озвучены
- [x] 140 новых записей для диалоговых карточек 29 уроков без озвучки — id `dlg_<урок>_<n>q/a`, добавлены в манифест и озвучены
- [x] 4 слова: חַג / טִיוּל / אוּלַי / אַז — пункт устарел, уже озвучены как `rw_54`–`rw_57.mp3` в `reading.js`, `audio: null` не найден
- [x] слова колод (462) — закрыто 08.08.2026: колонки `audio`/`gender` добавлены в `deck_words` (Supabase, миграция `add_audio_gender_to_deck_words`), 462 mp3 сгенерированы (edge-tts) и лежат в `public/reading/` рядом со всем остальным аудио, БД обновлена одним batch UPDATE. Род определялся авто-эвристикой (окончание ה/ת) + 29 ручных поправок на известные исключения (иврит: части тела без ж.р. окончания — יד/רגל/עין/אוזן/עצם/בטן/אצבע/שיניים; местоимения הוא/היא/אתה/את/הן/אתן; и т.п. — список поправок см. `git log` этого коммита). `api/decks.js` отдаёт новые поля без правок (нет `select=`, значит все колонки уже возвращаются).
  - ⚠️ Архитектурная заметка (обсуждали с пользователем 08.08.2026): контент колод осознанно живёт в Supabase (не в бандле) с этапа 5 — это позволяет редактировать без передеплоя. У уроков (`grammarLessons.js`/`reading.js`) контент в бандле — редактируется руками, требует ревью. Это два разных, оба валидных паттерна для разных типов контента; аудио-файлы при этом физически всегда лежат в одном месте (`public/reading/`) и играются одним и тем же плеером — в этом смысле архитектура уже единая.

**Найдено попутно, не озвучка — отдельный пробел:**
- [x] 500 базовых слов (`src/data/words.js`, `public/words499/`) технически все с audio, но **ни один компонент в `src/` не проигрывает это поле** — кнопка 🔊 нигде не подключена к этому списку слов. Решено 08.08.2026: легаси `words.js`/`public/words499/` удалены из гита целиком (был на 100% подмножеством `deck_words` по тексту — см. `PROMPT_DB_WORDS_ARCHITECTURE.md`), не переносим отдельно.
- [x] Слово №406 «קבלה» ссылалось на несуществующий `ankidroid_audiorec699113975.3gp` (артефакт импорта из Anki) — переозвучено через edge-tts, файл `500words_kabala.mp3`.

## 📦 Колоды (этап 5 — доработки)

- [x] **Баг**: `toItem()` в `DecksScreen.jsx` отдавал карточкам поля `he`/`ru`, а компонент флип-карточек (`CardsMode` в `ReadingScreen.jsx`) читает `item.hebrew`/`item.translation`/`item.audio` — из-за рассинхрона названий полей карточки колод при просмотре («Изучить») показывали пустое слово и пустой перевод. Исправлено 08.08.2026 — `toItem()` теперь отдаёт оба набора полей (he/ru для генераторов упражнений, hebrew/translation/transliteration/audio для CardsMode).
- [ ] Колоды не пишут SM-2 — подключить `planner.sm2` к `user_word_progress`
- [x] Расширить колоды за пределы 462 слов — сделано 08.08.2026 коммитом `3422537` («vocab: колоды по частям речи»), но НЕ тем источником, что был запланирован здесь (`bet.json`/10k apkg): вместо него — 4 новые колоды `g_nouns`/`g_adjectives`/`g_verbs`/`g_phrases`, источник «Hebrew-Russian_Bet__Audio + 100 TeachMeHebrew», реальное аудио (не TTS). Итого `deck_words` выросла с 462 до 5080 слов. Найдено 09.08.2026 при аудите (см. `VOCAB_ENRICHMENT_CANDIDATES.md`): пункт в бэклоге оставался открытым, хотя расширение уже произошло — правился только этот факт. ⚠️ Расширение ≠ интеграция: новые колоды по-прежнему физически изолированы от `reading.js`/линии курса (тот же разрыв, что и раньше); частотный импорт не курирован — `g_adjectives` 525/1678 без озвучки, `g_nouns` ~305/1553 похожи на нелеммы (артикль/союз-приставка) — тянет исходное предупреждение про лемматизацию, просто теперь с точными цифрами. Дальше — см. `VOCAB_ENRICHMENT_CANDIDATES.md` (Tier 1/Tier 2) и новый пункт ниже про интеграцию в линию курса.
- [x] Интегрировать Tier 1 (тематические колоды + разговорник) в линию курса — сделано 09.08.2026, 3 коммита (`0f25b69`, затем `a5194f0`). Итог: **242 тематических слова** (12 колод: body/city/clothes/colors/food/home/nature/numbers/study/time/transport/verbs) + **8 слов TV.family** + **86 фраз** (8 TV.phrases1 + 78 TV.phrases2) добавлены в `reading.js` как новые узлы `TV.*` и подключены в `COURSE_PATH` (`curriculum.js`) — открываются после конкретного грамматического урока уровней 1–4, прогресс не гейтят. Никуд — Dicta Nakdan API (batch-запросы по колоде/фразе, top-choice), **всё помечено `draft:true`** — вычитки человеком не было. Дедуп против существующего потока — по точному совпадению `plain`; при первом проходе фраз 3 дубля (точка в конце строки источника ломала точное сравнение) проскочили мимо фильтра и нашлись только смоук-тестом — убраны постфактум. 2 неизбежных омографа иврита без огласовок остаются (שער волосы/ворота, מלון/מילון гостиница/словарь) — задокументированы в тесте, не баг. `tests/smoke_v7.mjs` — все проверки зелёные (блоки «пилот», «партия2», «партия3»). Подробности и исходный список кандидатов — `VOCAB_ENRICHMENT_CANDIDATES.md`.
- [ ] **Вычитка `draft:true` контента партии обогащения** (242 слова + 86 фраз, см. выше) — Nakdan даёт статистически вероятный, но не гарантированно верный никуд без контекста; было как минимум 1 ручная правка найдена автоматической эвристикой (`מחברת`→`מַחְבֶּרֶת`, Nakdan предложил разбор с ошибочным предлогом). Построчная вычитка носителем/учителем не проводилась.
- [ ] **Аудит уже существующих (не-draft) огласовок в `reading.js` Nakdan'ом** — прогнать старые ~305 записей (216 слов + 73 фразы, до партии обогащения) тем же способом, что и новые, сравнить с текущим никудом, отчитаться о расхождениях (не исправлять автоматически — контент не draft, уже единожды вычитан вручную). Не начато.
- [ ] Tier 2 (4780 слов g_nouns/g_adjectives/g_verbs/g_phrases/conj/misc) — требует курации/лемматизации/доозвучки прежде чем трогать курс. Подробности — `VOCAB_ENRICHMENT_CANDIDATES.md`. ⚠️ 10.08.2026: порядок слов внутри g_nouns/g_adjectives/g_verbs/conj/g_phrases пересобран по смысловым подгруппам (см. `CHANGELOG.md`) — это НЕ курация/лемматизация, пункт остаётся открытым.
  - **Что имеем**: `conj` («служебные», 613 записей) размечен классификатором по ключевым словам на ~9 смысловых подгрупп (вопросительные/предлоги/местоимения и т.д., 46% покрытие) + группа «Прочее» (54%, ~330 записей, 41 чанк по 8) — то, что классификатор не смог отнести ни к одной подгруппе.
  - **Качество**: выборочная проверка 2 чанков «Прочее» (11.08.2026, группы 36 и 40 из decks-UI) — все 16 записей нерелевантны колоде «служебные»: топонимы (Европа, Азия), медицина (антибиотик, парацетамол), быт (степлер), религиозная лексика (Бог, аминь) в группе 36; словосочетания-наречия с בְּ- (силой, сердито, письменно, с честью) и «подмышка» в группе 40. Ни одного предлога/союза/частицы в обеих выборках.
  - **Суть проблемы**: это не сбой классификатора и не повод доразмечать «Прочее» точнее — сам источник данных колоды `conj` заведомо шире «служебных слов»: при импорте (см. пункт выше, 08.08.2026, `3422537`) в неё попала часть общей лексики, никак не связанной с грамматической функцией. Похожая картина подтверждена и для `g_adjectives` (профессии/родство вместо прилагательных). Пересортировка по подгруппам это не лечит и лечить не может — она работает поверх уже засорённого набора.
  - **План** (не начат, отложено): 1) аудит состава `conj`/`g_adjectives` — прогнать переводы через POS-эвристику или ручную выборку, пометить записи, не подходящие под заявленный класс колоды; 2) для помеченных — решить адресно: перенести в подходящую колоду (g_nouns и т.п.), в `misc`, либо выбросить как артефакт импорта; 3) только после очистки состава есть смысл пересматривать смысловые подгруппы внутри `conj`/`g_adjectives` — до этого «Прочее» там будет означать «мусор», а не «слово без темы».
- [ ] Колода «Разное» (140 слов) — разбить на осмысленные темы (не входила в перегруппировку 10.08.2026 — только 5 грамматических колод, не тематические)
- [x] GameScreen топик «Слова» — легаси `WORD_CATEGORIES`/`words.js` удалён 08.08.2026. Заодно нашёлся баг: с schema v8 `stats.wordsStudied` хранит id из `readingProgress` (`rw_*`/`rp_*`), а не числовые id `words.js` — тема "Слова" молча producировала пустой пул для всех постмиграционных пользователей. Починено: источник — `READING_WORDS` + `stats.readingProgress.studied` (как на HomeScreen/ReadingScreen).
- [x] Единая схема словаря — новая таблица Supabase `vocab_items` (RLS включён сразу, доступ только через service role) объединяет `reading.js` (289 items, source=core-course) и `deck_words` (462, source=legacy-decks) под одной схемой полей; `tools/vocab/sync_vocab_items.mjs` — идемпотентный синк; `tools/tts/generate_tts.py` — единый TTS-пайплайн для обоих источников, идемпотентность теперь по hash(текст+род+голос+темп), а не только по id+род (чинит баг «поправил текст — старое аудио осталось»). ReadingScreen/LessonScreen/decks.js пока не переведены на чтение из `vocab_items` — следующий шаг, не в этой задаче. Подробности и trade-off'ы обсуждения — `PROMPT_DB_WORDS_ARCHITECTURE.md`.

## 🟡 Продукт

- [ ] **v1.2 (часть 3)** — бейджи/достижения за прохождение экзамена подгруппы (v1.2 часть 1 — модули пути + подгруппы + 10 узлов-экзаменов, часть 2 — редизайн Home — обе готовы 09.08.2026, не закоммичено, см. `CHANGELOG.md`); связка «протухшего» модуля (SM-2, `planner.js`) с индикатором на свёрнутом модуле пути — открывает `CardsScreen`, отдельный экран не заводить
- [ ] Уведомление о разблокировке раздела
- [ ] Rate limiting `/api/chat`
- [ ] AI-ассистент PRO (~500 Stars/мес) · Прописи `writing_lifetime` (149) — заблокировано текущим статусом AI (`AI_ENABLED=false`, см. «Новое» выше)
- [ ] Монетизация P/R-модулей — решить free/Stars
- [ ] `getDueWords()` → очередь «Повторить»; судьба `recordWordSeen`/`recordWordResult`

## ✍️ Прописи (WritingScreen) — `writing_lifetime` 149 Stars

Stroke order (22 буквы), трассировка, диктант. Canvas + PointerEvents,
поле `writingProgress` в StatsContext.

## 🟢 Когда-нибудь

- [ ] Лидерборд · PWA-офлайн · индексы Supabase (`updated_at`, `created_at`)
- [ ] Standalone web (Stripe/LemonSqueezy) · мнемоники (v1.2 часть 2, 09.08.2026, поставила честную заглушку — «Как запомнить» на Home сейчас просто раскладывает новое слово по звукам уже известных букв, `HomeScreen.jsx:tipForNode`; настоящие ассоциативные мнемоники — ручной контент, не вывести из существующих данных) · фраза дня в пуше
- [ ] Ситуативные диалоги (магазин / автобус / ульпан)
- [ ] Чистка em-dash по всем докам и текстам приложения (в этой сессии новые строки уже на дефисе)
- [ ] В `Stickers/Pack-1` (вне репозитория, `C:\Users\dadab\Desktop\hebrew-app-updated\Stickers\Pack-1`) — ещё 12 поз маскота (zzz/blink/whaaa/star/lomevin/ovdim/savlanut/chemodan и т.д.), в `public/mascot/` пока перенесены только 3 (`hi`/`heyy`/`star`, v1.2 часть 2) — под будущие места, где нужна другая эмоция кабанчика
